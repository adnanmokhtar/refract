# ops-dashboard

Internal operations dashboard. React + TypeScript, built with Vite, talking to `orders-api`.

Conventions this codebase is held to live in [`ai/conventions.md`](ai/conventions.md).
They are not aspirational — most of the tree already follows them, and the components
that do are the reference for the ones that do not.
