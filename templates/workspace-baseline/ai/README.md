# ai/ — Workspace-level knowledge base

Workspace-level docs live here. Per-repo docs live in each sibling's own `ai/`.

## Files

- `architecture.md` — cross-repo system design (how repos connect, shared DB, event contracts, auth flow)
- `conventions.md` — cross-repo conventions (i18n key naming, DTO versioning, commit conventions)

Per-repo specifics belong in each sibling's `ai/`, not here.
