#!/usr/bin/env bash
# Git hook shim — learning loop (append-only changelog + review queue).
set -euo pipefail
ROOT="$(git rev-parse --show-toplevel 2>/dev/null)" || exit 0
exec "$ROOT/.claude/hooks/post-commit-learn.sh"
