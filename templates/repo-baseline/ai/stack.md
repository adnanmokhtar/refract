# Stack

## Runtime / Language
- <e.g. Node 20 / Python 3.12 / Go 1.22>

## Framework
- <e.g. NestJS 11 / Nuxt 4 / FastAPI>

## Package manager
- <e.g. bun / pnpm / uv / cargo>

## Database
- <e.g. MySQL 8 via TypeORM / PostgreSQL via Prisma>

## Key libraries
- <name@version — why it's here>

## Scripts
| Script | What it does |
|---|---|
| `bun run dev` | Start dev server |
| `bun run test` | Run tests |
| `bun run lint` | Lint |
| `bun run build` | Build for prod |

## Env files
- `.env` — local dev (gitignored)
- `.env.example` — template, committed
