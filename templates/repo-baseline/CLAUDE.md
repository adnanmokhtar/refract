# Project rules

The four foundational rules below are imported so they load every session via this
project `CLAUDE.md`. They are convention + the repo's real hooks — there is no CI gate
that ships with this baseline (enforcement detail lives in each rule's `## Enforcement`).

@.claude/rules/read-before-write.md
@.claude/rules/read-codebase-deeply.md
@.claude/rules/think-simplify-surgical.md
@.claude/rules/code-quality.md

Session bootstrap: read `ai/_session-digest.md` first (Tier 1). See `.claude/GUIDE.md`
for the full `.claude/` surface.
