{
  "kind": "pack",
  "name": "testing",
  "version": "1.2.0",
  "released": "2026-06-22",
  "min_setup_command": "3.0.0",
  "deprecated": false,
  "summary": "Sync-chain repair — _topics.md now declares run-tests as a command (it shipped under commands/ and is named in _essentials.md, but was absent from the topic list, so /setup-project AUTHOR-mode generation silently dropped it; no _examples/ stub exists so its fallback points at the live source). 1.1.0 unchanged: add-test Phase-6 mutation gate (break SUT → test must go red → restore) + optional --review (test-reviewer); /tdd command dispatching tdd-orchestrator (previously orphaned); flaky-test-hunt capture loop made runner-conditional (per-runner table); cross-links to /run-tests.",
  "changelog": {
    "1.2.0": {
      "released": "2026-06-22",
      "changes": [
        "Sync-chain repair: _topics.md now declares run-tests as a command (kind:command, test_framework_detected trigger). It shipped under commands/ and is listed in _essentials.md commands, but was absent from the topic list, so /setup-project AUTHOR-mode generation silently dropped it. No _examples/run-tests.md stub exists, so the fallback points at the live source (commands/run-tests.md)."
      ]
    }
  }
}
