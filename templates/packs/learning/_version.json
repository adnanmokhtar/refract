{
  "kind": "pack",
  "name": "learning",
  "version": "1.4.0",
  "released": "2026-08-20",
  "min_setup_command": "3.0.0",
  "deprecated": false,
  "summary": "NEW /recall — retrieval over the memory the loop already writes. Adds no sink, no store, no second index: one more row producer for the existing stdlib BM25 engine, plus an opt-in UserPromptSubmit hook that surfaces the don't-retry catalog before the approach is retried.",
  "changelog": "CHANGELOG.md"
}
