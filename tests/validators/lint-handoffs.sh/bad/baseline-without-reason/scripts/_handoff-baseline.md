# Handoff baseline (fixture)

A line with no trailing `# reason` suppresses nothing — the finding stays red.

## Backlog

```
docs/guide.md::templates/rules/thing.md::canonical-paths  SECTION-ANCHOR
```
