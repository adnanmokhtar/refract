# Phase 2 — demo

#### 2.0.a Shape handoff

Pass, verbatim from the Phase 1 output, alongside `project_root`:

```
repo_shape:   <single | monorepo>
members:
  - name: <member-a>
```

The receiver is the `demo-extract` skill; its Step 2 consumes the list.
