# The catalog shape changed and no wired row parses any more — the gate must say so
# rather than report a clean run over zero servers.
register_recommendation "filesystem" "@modelcontextprotocol/server-filesystem"
