# Fixture: cursor adapter (abridged)

`hooks.json` uses schema `version: 1` -- the only discriminator the format carries.
Events: `preToolUse`, `postToolUse`, `sessionStart`, `sessionEnd`.
Cursor publishes no schema-URL key, so none is emitted.
