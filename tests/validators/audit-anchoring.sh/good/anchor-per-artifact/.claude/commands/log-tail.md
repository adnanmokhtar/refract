---
name: log-tail
description: fixture — anchor carries a per-artifact relevance line
---
# /log-tail

<!-- project-specific:start -->
## Project-specific (anchored)
> - **Architecture** (`src/app.ts:1`): layered router → service → repository flow.
> - **Data access** (`src/app.ts:1`): a single DataSource is configured at bootstrap.
> - **Where this applies here** (`log-tail`): `src/app.ts:1`, 1 file(s) in src
> Cite-able sources: `package.json`, top-level: `src/`.
<!-- project-specific:end -->

Same five shared facts as its siblings, PLUS the one line only this artifact can have.
That single differing line is what separates per-artifact anchoring from a global
constant, and it is what the uniqueness census in audit-anchoring.sh measures.
