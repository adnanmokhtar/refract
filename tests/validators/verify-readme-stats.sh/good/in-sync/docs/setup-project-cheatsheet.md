# Fixture cheatsheet

Hand-maintained doc that restates corpus counts in prose. Pins check 7 of
verify-readme-stats.sh: the figures below match this mini-repo's disk (6 pack commands,
3 agents), so the gate must accept it.

## See also

- `templates/packs/` — pack catalog (2 tracks, 6 commands, 3 agents) + per-track `_essentials.md` manifests.
