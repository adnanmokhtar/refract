---
description: Rank what is wrong with existing code. Triggers — 'pre-launch sweep' is ambiguous; ask which axis is meant, since finish work is /polish. Run /clear first if the session is long.
allowed-tools: [Read, Write, Edit, Grep, Glob, Bash]
---
Body.
