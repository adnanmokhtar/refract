---
description: Enforce conventions the project already has. Triggers — 'align the app to the design system'. Do NOT trigger when every instance must go through one shared wrapper; that is /unify-surfaces.
allowed-tools: [Read, Write, Edit, Grep, Glob, Bash]
---
Body.
