---
description: Enforce conventions the project already has. Triggers — 'align the app'. Do NOT trigger when one shared wrapper must be extracted; that is /unify-surfaces.
allowed-tools: [Read, Write, Edit, Grep, Glob, Bash]
---
Body — /unify-surfaces was renamed and this pointer was left behind.
