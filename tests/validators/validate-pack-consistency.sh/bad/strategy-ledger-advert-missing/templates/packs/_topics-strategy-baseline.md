# `_topics.md` fallback-strategy ledger

This ledger is short on purpose. (The `**The ledger is N entries.**` sentence has been DELETED —
which used to be a cheaper edit than correcting it, and returned EXIT=0.)

## Ledger

```
p/do-foo   STUB-OVER-SOURCE  # the skeleton is the better delivery here
```
