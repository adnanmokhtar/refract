# `_examples/` fallback-integrity baseline

It is short on purpose. (The `**The backlog is N lines.**` sentence has been DELETED — which used
to be a cheaper edit than correcting it, and returned EXIT=0.)

## Backlog

```
(empty)
```
