# fixture baseline — pins that a declared, reviewed defect is suppressed rather than red.

**The backlog is 1 lines.** The count sentence is mandatory whenever this file exists (an existing
baseline that advertises nothing is a FAIL), so this fixture also pins that a present-and-correct
sentence is accepted.

```
p/foo   UNSOURCED-MAGNITUDE   # fixture: reviewed and accepted on purpose
```
