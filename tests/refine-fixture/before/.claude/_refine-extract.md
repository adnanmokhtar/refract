# Refine extraction — round two

> Generated by /setup-project --refine on 2026-04-15.
> Inputs: project at tests/refine-fixture/before, last commit (synthetic), prior setup version 1.0.0.

## Domain entities

### Domain: refine-fixture (synthetic)

- **`User`** at `src/models/user.py:14`
  - fields: id (uuid, pk), email (str, unique), created_at (datetime)
  - invariants: email is unique; lowercased before write
  - lifecycle_events: [user.created, user.email_changed]

- **`Session`** at `src/models/session.py:9`
  - fields: id (uuid, pk), user_id (fk → User), expires_at (datetime)
  - invariants: expires_at > created_at
  - lifecycle_events: [session.created, session.expired]

- **`Tenant`** at `src/models/tenant.py:6`
  - fields: id (uuid, pk), slug (str, unique), name (str)
  - invariants: slug is URL-safe (lowercase, hyphenated)

cross_entity_invariants:
- Every `Session` must reference a `User` whose `Tenant` is active.

## Architecture

Layers (top → bottom):
- api (controllers)  → src/api/
- service             → src/services/
- repository          → src/repos/
- model               → src/models/

Bounded contexts:
- `auth` (api/auth.py + services/auth.py + repos/user.py)
- `tenancy` (middleware/tenant.py + services/tenant.py + repos/tenant.py)
- These two contexts NEVER import each other directly; tenancy injects via middleware.

Representative request lifecycle (login):
- `src/api/auth.py:42` (POST /login)
  → `src/services/auth.py:18` (authenticate)
    → `src/repos/user.py:33` (find_by_email)
      → DB query with tenant_id filter

## Flows

### auth-login
- trigger: HTTP POST /login
- entry: `src/api/auth.py:42`
- steps:
  - `src/services/auth.py:18` (authenticate)
  - `src/repos/user.py:33` (find_by_email)
  - `src/services/session.py:9` (create_session)
- side_effects: writes Session, increments login_count
- error_paths: invalid_creds → 401; rate_limit → 429
- idempotency: not idempotent (each call creates a new Session)
- concurrency_mode: sequential await

### tenant-resolve
- trigger: every authenticated request
- entry: `src/middleware/tenant.py:18`
- steps:
  - `src/repos/tenant.py:7` (find_by_slug)
- side_effects: sets request.tenant
- concurrency_mode: sequential await

## Conventions (emergent)

- error-shape: every controller catches `DomainError` and rethrows as `HTTPException(status_code=400, detail=str(e))` (recur: 12 occurrences; sample `src/api/auth.py:55`).
- pagination: every list endpoint uses `cursor` (recur: 5 occurrences; no `limit/offset` anywhere).
- transaction-boundary: opened in service layer (recur: 8 occurrences; sample `src/services/auth.py:24`).
- async-work-naming: jobs live under `src/jobs/*.py`; never `tasks/`.

## Hot paths

### auth-login
- file: `src/api/auth.py:42`
- concurrency_mode: sequential await
- n+1_risk: none (single user lookup)
- index_coverage: covered (email index)
- cache_layer: absent
- uplift: looks healthy

### list-tenant-members
- file: `src/api/tenant.py:88`
- concurrency_mode: sequential await
- n+1_risk: high — loops `User.objects.get(id=member.user_id)` per member
- n_plus_1_sites: ["src/api/tenant.py:96"]
- index_coverage: covered
- cache_layer: absent
- uplift: replace per-member fetch with `User.objects.filter(id__in=ids)` batched query

## Failure history

### recurring_themes:
- **`tenant-bleed`**: 3 incidents over 9 months; root-cause family: `missing-tenant-filter`; affected files: src/repos/user.py, src/repos/order.py; representative commits: abc123def, def456abc.
