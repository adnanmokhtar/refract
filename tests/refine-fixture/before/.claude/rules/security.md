---
name: security-rule
description: Security rule — DEEP round-one anchor for testing (should be LEAVE-DEEP-IDEMPOTENT).
---

# Security rule

Validate inputs at the boundary. Use the project's `validate_input` helper.

<!-- project-specific:start -->
## Project-specific (refine-fixture)

> Auto-generated by /setup-project Phase 4.6 on 2026-04-01.

**Where this applies in refine-fixture** (round-one summary):
- `validate_input` helper at `src/security/validate.py:12`
- `AuthMiddleware` at `src/middleware/auth.py:8` (4 extenders)
- Tenant scoping via `TenantContext` at `src/tenancy/context.py:23`

**Domain entities involved**:
- `User` at `src/models/user.py:14` — invariant: email is unique
- `Session` at `src/models/session.py:9` — invariant: expires_at > created_at
- `Tenant` at `src/models/tenant.py:6` — invariant: slug is URL-safe

**Hot paths to watch**:
- `auth-login`: `src/api/auth.py:42` — N+1 risk: no, indexed lookup
- `tenant-resolve`: `src/middleware/tenant.py:18` — index covered

**Failure themes to avoid**:
- `tenant-bleed`: 3 incidents; root-cause family: `missing-tenant-filter` — see `ai/failures/tenant-bleed.md`
<!-- project-specific:end -->

---

## User-authored note

This rule has been hand-tuned. Please don't refactor it.
